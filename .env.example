# MCP Server Configuration
MCP_SERVER_PORT=3001
NODE_ENV=development

# Authentication (Optional - for secured endpoints)
MCP_API_KEY=generate-secure-key-here

# LangGraph Agents Server
LANGGRAPH_API_URL=http://localhost:2024

# External APIs (if MCP tools call them directly)
SAFEWAY_API_KEY=your-safeway-api-key

# Optional: LangSmith Tracing
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=lsv2_...
LANGCHAIN_PROJECT=shopping-assistant-mcp-dev

# Optional: Database (if MCP needs to query data)
DATABASE_URL=postgresql://user:password@localhost:5432/db
